<% if !user_signed_in? %>
    <div class="jumbotron">
        <h2>Las películas que te gusta ver</h2>
        <p><!-- start slipsum code -->
          - Déjenme hacerles una pregunta. ¿Y si no hubiera un mañana?<br>
          - ¿Si no hubiera un mañana? Entonces no habría consecuencias. No tendría resaca.<br>
          - Podríamos hacer lo que quisiéramos.<br>
          <p><i><a href="http://www.imdb.com/title/tt0107048/?ref_=fn_al_tt_1" target="_new">Atrapado en el tiempo</a></i> (1993)</p>
        <!-- please do not remove this line -->
        <div style="display:none;">
        <a href="http://slipsum.com">lorem ipsum</a></div>
        <!-- end slipsum code -->
        </p>
        <p><%= link_to "Registrarse", new_user_registration_path, class: "btn btn-primary btn-lg" %>
        <%= link_to "Login", new_user_session_path, class: 'btn btn-primary btn-lg'  %></p>
    </div>
<% end %>

<div id="pelis" class="row list-group">
    <% @movies.each do |movie| %>
    <div class="movie col-sm-4">
      <div class="thumbnail">
          <%= link_to (image_tag movie.image.url(:medium), class: 'thumbnail'), movie %>
          <span><strong><%= movie.title %></strong></span><br>
          <span><%= movie.fechaestreno %></span>
      </div>
      </div>
    <% end %>
</div> <!-- list-group-item -->
<br>

<%= link_to 'Añadir peli', new_movie_path, class: 'btn btn-primary' %>
